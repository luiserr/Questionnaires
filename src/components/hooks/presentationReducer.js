import React from 'react'

const reducerPresentationObject = (state, payload) => ({

}); 

export const presentationReducer = (state, action) => {
    //reducerPresentationObject(state, action.payload)[action.type] || state;
}